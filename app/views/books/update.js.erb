$('.main-list').prepend('<%= j render @book %>');
var form = $('.n-form');
form.find('input[type=text]').val('');
form.find('textarea').val('');
form.find('option').removeAttr('selected');
$('.n-modal').modal('toggle');
